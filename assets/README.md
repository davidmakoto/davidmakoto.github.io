# Project media

Images referenced by the site:

| File | Used by |
|---|---|
| `stem-voodoo-logo.png` | original supplied logo (source) |
| `stem-voodoo-logo-trim.png` | STEM Voodoo card — trimmed to bbox, transparent bg |
| `stem-voodoo-banner-dark.png` | unused — dark-theme logo variant |
| `booky-logo.png` | original supplied logo (source) |
| `booky-logo-trim.png` | Booket CRM card — trimmed to bbox, transparent bg |

Still missing: a `buddy-buddy` logo or screenshot, and screenshots for the
detail pages in `projects/` (`stem-voodoo.png`, `booket-crm.png`,
`buddy-buddy.png`). Where a file is absent the page shows a labelled
placeholder rather than a broken image.

Cards crop to 3:2 and detail heroes to 16:9. Logo lockups use the `is-logo`
class instead, which fits the mark inside the tile with padding rather than
cropping it.
